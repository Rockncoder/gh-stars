import { BackColorDirective } from './back-color.directive';

describe('BackColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackColorDirective();
    expect(directive).toBeTruthy();
  });
});
